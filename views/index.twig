{% extends 'layout.twig' %}

{% block body %}

  <h1>{{title}}</h1>
  <hr>
  <p>Welcome to {{title}}</p>
  <div id="vue"></div>
  {% for v in process.env|keys %}
        <li>{{ v }} == {{ process.env[v] }}</li>
    {% endfor %}

    {{ process.env.NODE_ENV|default("development") }}
{% endblock %}
